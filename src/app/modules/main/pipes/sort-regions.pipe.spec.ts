import { SortRegionsPipe } from './sort-regions.pipe';

describe('SortRegionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRegionsPipe();
    expect(pipe).toBeTruthy();
  });
});
