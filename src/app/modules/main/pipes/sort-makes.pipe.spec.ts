import { SortMakesPipe } from './sort-makes.pipe';

describe('SortMakesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMakesPipe();
    expect(pipe).toBeTruthy();
  });
});
