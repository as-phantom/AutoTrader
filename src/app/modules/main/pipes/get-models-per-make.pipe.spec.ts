import { GetModelsPerMakePipe } from './get-models-per-make.pipe';

describe('GetModelsPerMakePipe', () => {
  it('create an instance', () => {
    const pipe = new GetModelsPerMakePipe();
    expect(pipe).toBeTruthy();
  });
});
